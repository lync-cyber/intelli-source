# yaml-language-server: $schema=../schema/pipeline.schema.json
name: push-optimize
mode: flexible
max_steps: 10
tools_allowed:
  - truncate_for_push
  - filter_sensitive
  - llm_complete
tools_denied: []
steps: []
