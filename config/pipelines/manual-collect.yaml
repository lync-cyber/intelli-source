name: manual-collect
mode: strict
max_steps: 20
on_failure: abort
tools_allowed:
  - collect
  - process
  - distribute
tools_denied: []
steps:
  - tool: collect
    params:
      source_type: manual
  - tool: process
    params:
      pipeline: default
  - tool: distribute
    params:
      channels: all
