# yaml-language-server: $schema=../schema/pipeline.schema.json
name: scheduled-collect
mode: strict
max_steps: 20
on_failure: retry
tools_allowed:
  - collect
  - process
  - distribute
  - regex_extract
  - fingerprint_generate
  - fingerprint_dedup
  - tfidf_keywords
  - summarize_cluster
  - keyword_tag
  - filter_sensitive
tools_denied: []
steps:
  - tool: collect
    params: {}
  - tool: process
    params: {}
  - tool: distribute
    params: {}
