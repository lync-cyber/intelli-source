{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "DeduplicationResult",
  "description": "Schema for LLM semantic deduplication judgment result.",
  "type": "object",
  "properties": {
    "is_duplicate": {
      "type": "boolean",
      "description": "Whether the content is a duplicate of any candidate."
    },
    "confidence": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0,
      "description": "Confidence score for the duplication judgment."
    },
    "matched_candidate_id": {
      "type": ["string", "null"],
      "description": "ID of the matching candidate, if any."
    }
  },
  "required": ["is_duplicate", "confidence"],
  "additionalProperties": true
}
